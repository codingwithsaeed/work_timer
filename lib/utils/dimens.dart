abstract final class Dimens {
  const Dimens._();
  static const zero = 0.0;

  // Padding
  static const xsPadding = 4.0;
  static const sPadding = 8.0;
  static const mPadding = 16.0;
  static const lPadding = 24.0;
  static const xlPadding = 32.0;

  // IconSize
  static const xsIconSize = mPadding;
  static const sIconSize = lPadding;
  static const mIconSize = xlPadding;
  static const lIconSize = xlPadding + mPadding;
  static const xlIconSize = xlPadding + xlPadding;
}
